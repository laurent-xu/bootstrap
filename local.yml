---
- hosts: localhost
  connection: local
  become: true # enable root

  tasks:
  - name: install packages
    package:
      name:
        - htop
        - tmux
        - fzf
        - vim
        - git
        - zsh
        - terminator
        - chromium-browser
